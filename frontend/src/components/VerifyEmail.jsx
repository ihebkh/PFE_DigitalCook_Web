// VerifyEmail.jsx
// Page de vérification d'email (layout + formulaire)
import AuthLayout from '../layouts/AuthLayout';
import VerifyEmailForm from './VerifyEmailForm';

/**
 * Page de vérification d'email (layout + formulaire).
 */
export default function VerifyEmail() {
  return (
    <AuthLayout>
      <VerifyEmailForm />
    </AuthLayout>
  );
} 